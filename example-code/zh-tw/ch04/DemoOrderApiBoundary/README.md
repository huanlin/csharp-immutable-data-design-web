# DemoOrderApiBoundary

This ASP.NET Core 10 project demonstrates immutable request and response DTOs,
Minimal API validation, explicit mapping to a domain model, and an
immutable settings snapshot created from a mutable options binding model.

Run:

```powershell
dotnet run --project .\DemoOrderApiBoundary.csproj
```
